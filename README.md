# StreamDeck

A macOS-inspired desktop environment in the browser, built with React and Vite.

## Getting Started

Install dependencies:

```sh
npm install
```

## Developing

Start a development server:

```sh
npm run dev
```

## Building

Create a production build:

```sh
npm run build
```

Preview the production build:

```sh
npm run preview
```
